# Set to your deployed backend URL in production (e.g. https://argus-backend.up.railway.app)
VITE_API_BASE=http://localhost:8000

# Set true ONLY to develop against local JSON fixtures with no backend running
VITE_USE_MOCKS=false
