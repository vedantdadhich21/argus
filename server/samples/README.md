# ⚠️ Sample APKs — handle with care

- `fake_banker.apk` / `benign_notes.apk` are **self-built** demo apps (Reference §16) — safe, deterministic, no real malware.
- Any real-world samples downloaded from MalwareBazaar etc. must **never be installed on a device** and only analyzed inside the Docker container.
- `*.apk` files here are gitignored. Never commit binaries.
- Delete all samples after the event.
